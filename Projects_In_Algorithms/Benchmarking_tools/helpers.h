// ========================================================================
// File: helpers.h
//
// Description:
//  Contains ALL function declarations of the functions implemented in
//  the file "helpers.cpp".  Some of these functions are the actual
//  sorting functions.
// ========================================================================
std::vector<int> h_sort1(std::vector<int>& nums, int h);
std::vector<int> h_sort2(std::vector<int>& nums, int h);
std::vector<int> h_sort3(std::vector<int>& nums, int h);
std::vector<int> generate_sequence(const std::vector<int> &in_nums);
std::vector<int> make_pratt_list(const int in_size);
std::vector<int> merge(std::vector<int> inV1, std::vector<int> inV2);
std::vector<int> m_sort(const std::vector<int>& nums);
